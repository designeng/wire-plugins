define ->
    # all specs must be in folder defined in SpecRunner.pathToSpec
    specs = [
        "core/plugin/data/follow"
        "core/plugin/data/bunch"
        "core/plugin/data/structure/collection"
        "core/plugin/template/hbsResolver"
    ]
